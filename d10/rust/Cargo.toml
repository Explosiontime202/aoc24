[package]
name = "day10"
version = "0.1.0"
edition = "2021"

[dependencies]
rustc-hash = "2.1.0"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "benchmark"
harness = false
